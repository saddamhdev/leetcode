এবার **Symmetric Tree (LeetCode 101)** একদম **চোখের সামনে ভিজ্যুয়াল + পুরো step-by-step dry run** করে বুঝিয়ে দিচ্ছি।
এটা বোঝলে `mirror`, `DFS`, `recursion`—সব ক্লিয়ার হয়ে যাবে।

---

# 🌟 **Symmetric Tree — LeetCode 101 (Easy)**

### ❓ Problem:

একটা binary tree দেওয়া আছে।
Check করতে হবে → **গাছটা কি নিজের mirror image?**

মানে:

```
Left subtree == Right subtree (mirror আকারে)
```

---

# 🌳 Example of Symmetric Tree

```
        1
       / \
      2   2
     / \ / \
    3  4 4  3
```

এটা symmetric কারণ বাম দিক ↔ ডান দিক mirror image।

---

# 🚫 Not Symmetric Example

```
        1
       / \
      2   2
       \   \
        3   3
```

Shape একই না → symmetric না।

---

# 🧠 Core Idea (Important)

Tree symmetric মানে:

👉 **Left subtree mirror of Right subtree**

Mirror মানে:

* left.left ↔ right.right
* left.right ↔ right.left
* value same হওয়া লাগবে

---

# ✔ Java Code (Mirror comparison)

```java
class Solution {
    public boolean isSymmetric(TreeNode root) {
        return isMirror(root.left, root.right);
    }

    boolean isMirror(TreeNode a, TreeNode b) {
        if (a == null && b == null) return true;
        if (a == null || b == null) return false;

        if (a.val != b.val) return false;

        return isMirror(a.left, b.right) &&
               isMirror(a.right, b.left);
    }
}
```

---

# 🔍 Step-by-Step Dry Run (Very Important)

ধরি tree হলো:

```
        1
       / \
      2   2
     / \ / \
    3  4 4  3
```

Start:

```
isSymmetric(root)
→ isMirror(root.left=2, root.right=2)
```

---

## 🔹 Step 1: Compare (2, 2)

```
a = 2
b = 2
values equal → OK
```

Now check mirror:

```
isMirror(a.left=3,  b.right=3)   → left-left vs right-right
isMirror(a.right=4, b.left=4)   → left-right vs right-left
```

Let's check them:

---

## 🔹 Step 2: Compare (3, 3)

```
a = 3
b = 3
values equal → OK
```

3 is leaf, both children null:

```
isMirror(null, null) → true
isMirror(null, null) → true
```

So whole pair (3,3) → **true**

---

## 🔹 Step 3: Compare (4, 4)

```
a = 4
b = 4
values equal → OK
```

Same as before:

```
isMirror(null, null) → true
isMirror(null, null) → true
```

So whole pair (4,4) → **true**

---

## 🔹 Back to root level:

```
Left side result  = true (3 ↔ 3)
Right side result = true (4 ↔ 4)

Final = true && true = true
```

✔ Entire tree symmetric!

---

# 🔥 Dry Run of a Not Symmetric Tree (Important Case)

Tree:

```
        1
       / \
      2   2
       \   \
        3   3
```

Start:

```
isMirror(2,2)
```

### Step 1:

Compare children:

```
isMirror(a.left=null, b.right=3)
```

এখানে:

* a.left = null
* b.right = 3

```
(a==null || b==null) → true
return false
```

So final output: **false**

Tree symmetric না।

---

# 🧠 Concept Summary (1-minute interview answer)

> “A tree is symmetric if left subtree is a mirror of the right subtree.
> I recursively compare:
>
> * left.left with right.right
> * left.right with right.left
>   If both sides match at every level, then the tree is symmetric.”

---

# 🎯 Want next explanation?

আমি step-by-step explain করতে পারি:

* **Invert Binary Tree (226)**
* **Balanced Binary Tree (110)**
* **Path Sum (112)**
* **Lowest Common Ancestor (236)**

যেটা শিখতে চাও, বলো:
👉 **“Explain Invert Tree step-by-step”**
