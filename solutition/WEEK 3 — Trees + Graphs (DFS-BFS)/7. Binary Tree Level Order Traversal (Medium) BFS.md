চল একদম **লাইন বাই লাইন + ড্রাই রান** করে বুঝি 👇

```java
class Solution {
    public List<List<Integer>> levelOrder(TreeNode root) {
        List<List<Integer>> result = new ArrayList<>();
        if (root == null) return result;

        Queue<TreeNode> q = new LinkedList<>();
        q.add(root);

        while (!q.isEmpty()) {
            int size = q.size();
            List<Integer> level = new ArrayList<>();

            for (int i = 0; i < size; i++) {
                TreeNode node = q.poll();
                level.add(node.val);
                if (node.left != null) q.add(node.left);
                if (node.right != null) q.add(node.right);
            }
            result.add(level);
        }
        return result;
    }
}
```

---

## 1️⃣ মেথড সিগনেচার

```java
public List<List<Integer>> levelOrder(TreeNode root)
```

* ইনপুট: `root` → ট্রির রুট নোড
* আউটপুট: `List<List<Integer>>`

  * প্রতিটি **লেভেল** এর জন্য একটা `List<Integer>`
  * সব লেভেল মিলিয়ে `result` নামে বড় লিস্ট

উদাহরণ:
ট্রি যদি হয়:

```
      1
     / \
    2   3
   / \
  4   5
```

তাহলে আউটপুট হবে: `[[1], [2, 3], [4, 5]]`

---

## 2️⃣ result লিস্ট তৈরি

```java
List<List<Integer>> result = new ArrayList<>();
```

* শুরুতে `result` খালি: `result = []`

---

## 3️⃣ null চেক

```java
if (root == null) return result;
```

* যদি ট্রি-ই না থাকে (root == null)

  * সরাসরি খালি লিস্ট `[]` রিটার্ন করবে
* তাই নিচের কোড সব **non-empty tree** এর জন্য

---

## 4️⃣ Queue বানানো (BFS এর জন্য)

```java
Queue<TreeNode> q = new LinkedList<>();
q.add(root);
```

* `q` হলো Queue (FIFO)
* প্রথমে শুধু root ঢোকানো হয়:

`q = [1]` (ধরি root.val = 1)

---

## 5️⃣ বাইরের while – যতক্ষণ queue তে node আছে

```java
while (!q.isEmpty()) {
```

* যতক্ষণ queue খালি না হয়, ততক্ষণ লেভেল ধরে ধরে প্রসেস করবে

---

## 6️⃣ current level এর size নেওয়া

```java
int size = q.size();
List<Integer> level = new ArrayList<>();
```

* `size` = এই লুপে **এই লেভেলে** কয়টা নোড আছে
* `level` = এই লেভেলের সব নোডের value রাখার লিস্ট

### প্রথম iteration (Level 0):

* কিউ: `[1]`
* `size = 1`
* `level = []`

---

## 7️⃣ ভিতরের for – এই লেভেলের সব নোড প্রসেস

```java
for (int i = 0; i < size; i++) {
    TreeNode node = q.poll();
    level.add(node.val);
    if (node.left != null) q.add(node.left);
    if (node.right != null) q.add(node.right);
}
```

এখানে যা হচ্ছে:

### 🔹 Step A: `TreeNode node = q.poll();`

* `poll()` → কিউ থেকে **সামনের নোডটা বের করে আনে এবং সরিয়ে দেয়**
* প্রথম লেভেলে:

  * `node = 1`
  * কিউ এখন `[]`

### 🔹 Step B: `level.add(node.val);`

* এই লেভেলের লিস্টে ভ্যালু যোগ করলে:

  * `level = [1]`

### 🔹 Step C: চিল্ড্রেন কিউতে ঢোকানো

```java
if (node.left != null) q.add(node.left);
if (node.right != null) q.add(node.right);
```

ধরি:

* 1 এর left = 2
* 1 এর right = 3

তাহলে:

* `q.add(2);`
* `q.add(3);`
* কিউ এখন: `[2, 3]`

### এই লেভেলের for লুপ শেষ

(কারণ `size = 1`, তাই `i = 0` একবারই চলল)

---

## 8️⃣ লেভেলটাকে result-এ যোগ করা

```java
result.add(level);
```

* এখন:

  * `result = [[1]]`

---

## 🔁 দ্বিতীয় iteration (Level 1)

এখন আবার while লুপে ফিরি:

* কিউ: `[2, 3]`
* খালি না → while চালু

```java
int size = q.size();  // size = 2
List<Integer> level = new ArrayList<>(); // level = []
```

### for loop এবার ২ বার চলবে (i = 0, 1)

---

### ➤ i = 0 এর জন্য:

```java
TreeNode node = q.poll(); // node = 2, q = [3]
level.add(node.val);      // level = [2]
if (node.left != null) q.add(node.left);   // 4 add
if (node.right != null) q.add(node.right); // 5 add
```

* কিউ এখন: `[3, 4, 5]`

---

### ➤ i = 1 এর জন্য:

```java
TreeNode node = q.poll(); // node = 3, q = [4, 5]
level.add(node.val);      // level = [2, 3]
if (node.left != null) ...   // ধরো 3 এর left নেই
if (node.right != null) ...  // ধরো 3 এর right নেই
```

* কিউ থাকে: `[4, 5]`

### এই লেভেল শেষ → `result.add(level);`

* `result = [[1], [2, 3]]`

---

## 🔁 তৃতীয় iteration (Level 2)

* কিউ: `[4, 5]`
* `size = 2`
* `level = []`

### i = 0:

* `node = 4`, `q = [5]`
* `level = [4]`
* ধরো 4 এর কোনো child নেই

### i = 1:

* `node = 5`, `q = []`
* `level = [4, 5]`
* ধরো 5 এরও child নেই

```java
result.add(level); // result = [[1], [2, 3], [4, 5]]
```

---

## ❌ পরেরবার while চেক

```java
while (!q.isEmpty()) // এখন q খালি
```

* কিউ তো এখন `[]`
* `q.isEmpty()` → true
* তাই `!q.isEmpty()` → false
* while লুপ থেমে যাবে

---

## ✅ শেষ লাইন

```java
return result;
```

ফাইনাল আউটপুট:

```java
[[1], [2, 3], [4, 5]]
```

---

## এক কথায় সারসংক্ষেপ:

1. `result` = সব লেভেলের লিস্ট রাখবে
2. `q` = BFS করার জন্য কিউ
3. প্রতি while লুপ = **একটা লেভেল**
4. `size = q.size()` = এই লেভেলে কয়টা নোড
5. ভেতরের for = ওই লেভেলের সব নোড:

   * `poll()` করে বের করো
   * `level`-এ ভ্যালু রাখো
   * তার চিল্ড্রেন কিউতে ঢোকাও
6. লেভেল শেষ হলে `result.add(level)`
7. কিউ ফাঁকা হলে কাজ শেষ

---

চাও তো আমি এই একই কোডটা নিয়ে

* আরেকটা ট্রি দিয়ে dry run করে দেখাতে পারি,
  অথবা
* DFS version (recursive level-order) কিভাবে হবে সেটাও দেখাতে পারি 🙂
