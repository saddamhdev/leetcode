দারুণ প্রশ্ন!
**Java কখন copy পাঠায় আর কখন reference পাঠায় — এটা জানলে পুরো বিষয়টা crystal clear হয়ে যাবে।**

---

# ✅ **Java দুই ধরনের জিনিস পাঠাতে পারে:**

## ✔ 1) **Primitive Types → copy পাঠায়**

যেমন:

* int
* double
* boolean
* char
* float
* long
* byte
* short

**যখন primitive value method এ পাঠাও → এর copy যায়।**

### উদাহরণ:

```java
public void change(int x) {
    x = 100;
}

int a = 10;
change(a);

System.out.println(a);  // Output: 10
```

🔍 `a` এর copy গিয়েছিল → তাই `a` পরিবর্তন হয়নি।

---

## ✔ 2) **Objects → reference পাঠায় (copy of reference লিঙ্ক)**

Objects include:

* Arrays (`int[]`, `String[]`)
* StringBuilder
* Custom class objects
* List, Map, Set
* etc.

**এগুলোতে reference এর copy পাঠানো হয়**
→ মানে method এর ভেতরে object modify করলে main object একইভাবে modify হবে।

### উদাহরণ:

```java
public void change(int[] arr) {
    arr[0] = 100;
}

int[] nums = {1,2,3};
change(nums);

System.out.println(nums[0]); // Output: 100
```

🔍 কারণ arr এবং nums দুটিই একই array কে point করছে।

---

# 🚀 তাই তোমার মূল প্রশ্নের উত্তর:

## **📌 Java কখন copy পাঠায়?**

➡️ যখন primitive type পাঠাও।
➡️ তখন original value পরিবর্তন হয় না।

## **📌 Java কখন reference পাঠায়?**

➡️ যখন array বা object পাঠাও।
➡️ method এর ভিতরে পরিবর্তন করলে original object পরিবর্তন হয়।
➡️ Rotate Array code-এর case এ array পাঠানো হয়েছিল, তাই void হলেও কাজ হয়েছে।

---

# 🎯 Summary Table

| ডেটা টাইপ                  | Pass হয়                  | Original পরিবর্তন হয়? |
| -------------------------- | ------------------------ | --------------------- |
| int, double, char, boolean | **copy of value**        | ❌ না                  |
| Array                      | **copy of reference**    | ✅ হ্যাঁ               |
| Object                     | **copy of reference**    | ✅ হ্যাঁ               |
| String                     | immutable, reference যায় | ❌ modify করা যায় না   |

---

# 🔥 চাইলে আমি diagram দিয়ে দেখাতে পারি কিভাবে reference copy হয়।
