চলো **49. Group Anagrams** টা ধীরে ধীরে করি 🙂

---

## 🧠 Problem টা কী চায়?

Given:
`String[] strs` → যেমন:

```java
["eat","tea","tan","ate","nat","bat"]
```

তোমাকে এমনভাবে group করতে হবে যেন:

* যেগুলো **একই অক্ষর দিয়ে তৈরি (anagram)** সেগুলো **একই গ্রুপে** যায়।

Output (order নিয়ে tension নেই):

```java
[
  ["eat","tea","ate"],
  ["tan","nat"],
  ["bat"]
]
```

**Anagram** মানে →
`eat, tea, ate` → একই অক্ষর: `a, e, t`

---

## ✅ Main Idea (Sorting দিয়ে)

যেকোনো string যদি আমরা **sort করি**,
তাহলে anagram গুলোর sorted form **একই হবে**।

* `"eat"` → `"aet"`
* `"tea"` → `"aet"`
* `"ate"` → `"aet"`

তাই আমরা:

1. প্রতিটা string কে sort করব → এটাকে key বানাব
2. `HashMap<key, List<String>>` এ রাখব
3. শেষে map-এর সব value-গুলোই হবে আমাদের grouped anagrams

---

## ✅ Java Solution (Sorting ভিত্তিক – সহজ & স্ট্যান্ডার্ড)

```java
import java.util.*;

class Solution {
    public List<List<String>> groupAnagrams(String[] strs) {

        // Map: sorted string -> list of anagrams
        Map<String, List<String>> map = new HashMap<>();

        for (String s : strs) {
            // 1) String -> char array
            char[] chars = s.toCharArray();

            // 2) Sort the characters
            Arrays.sort(chars);

            // 3) Sorted chars -> String (this is our key)
            String key = new String(chars);

            // 4) Map এ key আছে কিনা চেক করো
            if (!map.containsKey(key)) {
                map.put(key, new ArrayList<>());
            }

            // 5) ওর group এ original string add করো
            map.get(key).add(s);
        }

        // সব value গুলা ই হচ্ছে group-anagrams
        return new ArrayList<>(map.values());
    }
}
```

---

## ⏱️ Complexity

* ধরি `n` string আছে, আর প্রতিটার গড় length `k`
* প্রতিটা string sort করতে লাগে `O(k log k)`
* মোট সময়: `O(n * k log k)`
* Extra space: `O(n * k)` (map + lists)

---

## 🔁 আরেকটা Way (Sorting ছাড়াই – Frequency Count দিয়ে)

যদি পরে চাও, আমি দেখাতে পারি:

* ২৬ সাইজের int array দিয়ে (a–z count)
* এটাকে key বানিয়ে anagram group করা
  → সময় হবে `O(n * k)` (sort-এর চেয়ে একটু efficient)

---

তুমি চাইলে আমি:

* Bangla explain + dry run দিতে পারি
* বা count-array ভিত্তিক আরেকটা version লিখে দিতে পারি

বলো, কোনটা next চাও? 👀
